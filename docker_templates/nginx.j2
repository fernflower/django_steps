# mysite_nginx.conf

upstream django {
    server unix:///django_steps/mysite/mysite.sock; 
}

# configuration of the server
server {
    # the port your site will be served on
    listen      {{NGINX_PORT}};
    server_name {{NGINX_HOST}}; # substitute your machine's IP address or FQDN
    charset     utf-8;

    # max upload size
    client_max_body_size 75M;   # adjust to taste

    rewrite ^.*/{{DJANGO_STATIC_URL}}(.*)/? /{{DJANGO_STATIC_URL}}$1 last;

    location /{{DJANGO_STATIC_URL}} {
        alias /django_steps/mysite/static/;
    }

    # media files for django-admin
    location /posts/media {
        alias /django_steps/mysite/media/;
    }

    location / {
        uwsgi_pass unix:///django_steps/mysite/mysite.sock; 
        include uwsgi_params;
    }
}
